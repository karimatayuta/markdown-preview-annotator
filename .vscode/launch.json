{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Markdown Preview Annotator",
      "type": "extensionHost",
      "request": "launch",
      "preLaunchTask": "build",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}"
      ]
    }
  ]
}
